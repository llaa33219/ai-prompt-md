# 영상 생성 가이드
## 1. 요청 파악
유저에게 기획안을 받는다.
---
[영상 요소의 url또는 파일 경로]
{배경 영상의 대략적 느낌}
(영상 요소의 대략적 배치)
현재 화면에 나올 느낌이나 뭐 내용이나 작성
---
뭐 이런식으로 물어보고 받아내면 되고, 자세히 있을수도 있고 대충 작성되어있을수도 있으니까 대충이면 알아서 강화시키고.

영상 비율이나 프레임레이트도 잘 물어보아야 한다.

영상 언어도 물어보아야 한다. 근데 여기는 음성은 나오지 않는 것이 기본이니, 나머지 언어를 뜻한다.
## 2. 영상 소스 생성 및 가공
배경에 들어갈 영상을 생성해야 한다.
### 2.1. 유저에게 프롬프트 제공
https://raw.githubusercontent.com/llaa33219/c3-image-bucket/refs/heads/main/image(9).png 이 이미지가 이미지 1로 첨부될때도 있다(첨부할지 말지는 1.에서 정할 것이다.).
배경은 매우 현실적인 느낌이어야 하지만 꼭 현실의 법칙을 지킬 필요는 없다.(드림코어, 리미널 스페이스 등)
사용자에게 제공할 프롬프트는 영어로 작성하며, 자연어 문장의 나열로 작성하여야 한다.
이것저것 자세히 명시하여야 한다. 카메라(영상에 나올 화면 설정)관련 설정도 잘 명시하여야 한다.

영상은 여러개 생성할 것이니, 프롬프트도 여러개 만들어져야 한다.
### 2.2. 제공받은 영상 가공
```
#!/usr/bin/env bash
# ═══════════════════════════════════════════════════════════════════
#  DD. Cine Lens — VIDEO  (M렌즈 + Astigmatism + Teal/Orange + 노이즈)
# ═══════════════════════════════════════════════════════════════════
#  사용법: bash apply_dd_video.sh <입력영상>
#  출력:   presets/DD_<입력파일명>.mp4
# ═══════════════════════════════════════════════════════════════════
set -e

INPUT="${1:-source.mp4}"
NAME=$(basename "$INPUT" | sed 's/\.[^.]*$//')
OUT="presets/DD_${NAME}.mp4"
mkdir -p presets

ffmpeg -y -i "$INPUT" -filter_complex "
  # ────────────────────────────────────────────────────────────
  # 0) 1280px 리사이즈 + 2갈래 분기 (a=본체, b=광원)
  # ────────────────────────────────────────────────────────────
  [0:v]scale=1280:-1,split=2[a][b];

  # ────────────────────────────────────────────────────────────
  # 1) [a] Astigmatism — 3픽셀 가로 평균 (수평 streak)
  # ────────────────────────────────────────────────────────────
  [a]geq=r='(r(X-2,Y)+r(X,Y)+r(X+2,Y))/3'
     :g='(g(X-2,Y)+g(X,Y)+g(X+2,Y))/3'
     :b='(b(X-2,Y)+b(X,Y)+b(X+2,Y))/3'[astig];

  # ────────────────────────────────────────────────────────────
  # 2) [b] Anamorphic Flare — 밝은 부분 가로 streak
  #    brightness +12% → boxblur × 5 (lr=300씩)
  # ────────────────────────────────────────────────────────────
  [b]eq=brightness=0.12,
     boxblur=lr=300,boxblur=lr=300,boxblur=lr=300,
     boxblur=lr=300,boxblur=lr=300[flare];

  # ────────────────────────────────────────────────────────────
  # 3) M Lens — 방사형 색수차 + 보라색 프린지
  #    ※ 좌표를 W/2, H/2 로 자동 스케일 (모든 해상도 대응)
  # ────────────────────────────────────────────────────────────
  [astig]geq=r='r(X-(X-W/2)*0.006-(X-W/2)*0.0015,
                Y-(Y-H/2)*0.006-(Y-H/2)*0.0015)'
         :g='g(X,Y)'
         :b='b(X+(X-W/2)*0.006+(X-W/2)*0.0015,
                Y+(Y-H/2)*0.006+(Y-H/2)*0.0015)'[ca];

  # ────────────────────────────────────────────────────────────
  # 4) [ca] + [flare] screen 합성 (opacity 0.20)
  # ────────────────────────────────────────────────────────────
  [ca][flare]blend=all_mode=screen:all_opacity=0.20[with_flare];

  # ────────────────────────────────────────────────────────────
  # 5) Cinematic Teal/Orange + 채도/콘트라스트 + 노이즈 + 비네팅
  # ────────────────────────────────────────────────────────────
  [with_flare]colorbalance=rs=0.08:bs=0.08:gs=-0.05,
              eq=saturation=1.08:contrast=1.08:brightness=-0.02,
              curves=preset=increase_contrast,
              noise=alls=35:allf=t,
              gblur=sigma=0.6,
              vignette=PI/6[v]
" -map "[v]" -map 0:a? \
   -c:v libx264 -preset medium -crf 18 -pix_fmt yuv420p \
   -c:a copy \
   -movflags +faststart \
   "$OUT"

echo "✓ Saved: $OUT"
```
이 코드를 참고하여 영상을 가공한다.
## 3. 영상 제작
### 3.1. 디자인 파악
https://raw.githubusercontent.com/llaa33219/design-style-ai/refs/heads/main/blp-minimal-infinite-tile(12).md 이 디자인 언어를 사용하여야 한다.
단, 배경은 2.에서 생성한 영상을 사용한다. 물론 비는 부분이 디자인 언어적으로 많기 때문에, 배경이 상당히 중요한 역할이다.
화면에 등장하는 요소는 단순하고 몇게 없어야 하며, 자세하게 만들면 안된다.(굳이 자세하게 하고싶으면 미디아 형식을 사용해서 넣어라.)
### 3.2. 영상 기획
정밀하고 깔끔하게 기획하며, 기획이 다 되면 사용자에게 수락을 받아라.
장면별로 나누고(배경 영상별로 다른 장면으로 하면 편하다.), 각 장면에 어떤 요소들을 어떻게 넣을지 생각하라. 장면은 워크스페이스별로(디자인 언어 참고) 나누어야 한다.
모든 장면은 선택된 언어로 제작한다. 사용자가 어떤 언어로 설계를 줬던간에 무조건 선택한 언어로 번역하거나 애초부터 그 언어로 작성한다.
### 3.3. 영상 생성
ffmpeg를 사용하여 영상을 생성한다.
적절한 배경음악 넣어주고.
기획에서 기획한걸 장면별로 생성 후 합치고, 배경음악을 넣는다.
### 3.4. 영상 제공
mp4로 줘라.
